# 2026-05-19 — update-clam-2021

User invoked `/add-article` on the CLAM paper, which was already ingested as `clam-2021` during the cluster-2 batch (see `2026-05-19-cluster2-mil.md`). Per the article-ingestion skill ("Do Not Use When: the article is already in `knowledge/articles/`"), no re-ingestion. Instead, **enriched the existing record** with material reachable from PubMed + Crossref reference list.

## Added Objects

None — this is an enrichment of an existing node.

## Updated Objects

`knowledge/articles/technical/clam-2021.yaml`:

- `links.pubmed`: `to_verify` → `"33649564"` (resolved via PubMed esearch on DOI).
- `links.pdf`: added open-access PMC URL `https://www.ncbi.nlm.nih.gov/pmc/articles/PMC8711640/` (PMCID confirmed via efetch).
- `abstract`: replaced synthesized one-paragraph summary with the **real PubMed-indexed abstract**. The new abstract highlights two claims absent from the original synthesised version:
  - generalisation to **independent test cohorts** + **smartphone microscopy** + **varying tissue content**;
  - framing CLAM as solving "poor domain adaptation and interpretability" — explicit DA framing.
- `tasks`: appended `domain-adaptation`, `cross-magnification-evaluation` to reflect the smartphone-microscopy / cross-cohort evaluation chapters of the paper.
- `related_articles`: added hint references to `campanella-2019` (cited as the clinical-grade weakly-supervised MIL precedent, ref 36) and `hover-net-2019` (cited as the nuclei-segmentation reference, ref 10).

`content/articles/technical/clam-2021.md` (EN):

- New section **"Beyond standard benchmarks"** highlighting the three domain-adaptation claims (independent cohorts, smartphone microscopy, varying tissue content). These were not in the original synthesised page.
- References section now includes PubMed and PMC links and full venue/volume/page citation.

`content/articles/technical/clam-2021.zh.md` (ZH): mirror of the EN changes — same "超越标准基准" section, same expanded references.

## Added Graph Edges

2 new edges in `knowledge/graph/edges.yaml`:

| from | type | to | confidence | rationale |
|------|------|----|------------|-----------|
| `clam-2021` | `related_to` | `campanella-2019` | high | CLAM ref 36 — explicit predecessor citation; CLAM positions itself as data-efficient successor to clinical-grade Campanella 2019. |
| `clam-2021` | `related_to` | `hover-net-2019` | medium | CLAM ref 10 — contemporaneous nuclei-segmentation reference, methodologically tangential. |

Existing edges around `clam-2021` retained: `proposes_method → clam`, `extends → abmil-2018`, `uses_dataset → camelyon16`, plus incoming from `transmil-2021` (compares_with), `bahadir-2024` (compares_with).

Graph stats: **31 nodes, 86 edges** (was 31 / 84). No orphans.

## Reference-list mining (Crossref, 47 refs total)

Confirmed cross-references to existing nodes:

- ref 10 — Graham et al. HoVer-Net → `hover-net-2019` ✓ (edge added)
- ref 36 — Campanella et al. → `campanella-2019` ✓ (edge added)
- ref 37 — Ilse et al. ABMIL → `abmil-2018` (edge `extends` already present)
- ref 46 — Litjens et al. Camelyon → `camelyon16` (edge `uses_dataset` already present, evidence string updated)

References not yet ingested but high-value future candidates (consistent with CLAUDE.md §11.6 — surfacing, not auto-stubbing):

- ref 4 — Kather et al. MSI from histology (Nat Med 2019)
- ref 5 — Bulten et al. Gleason Lancet Oncol 2020 (related to `panda` Bulten-2022 paper)
- ref 17, 18 — Fu, Kather pan-cancer biomarker prediction (Nat Cancer 2020)
- ref 31 — Bejnordi et al. Camelyon JAMA 2017 (already in deferred list as `bejnordi-2017-camelyon16`)
- ref 32 — Chen et al. AR microscope (Nat Med 2019)
- ref 35 — Coudray et al. NSCLC mutation prediction (Nat Med 2018)

## Validation Results

```
$ python scripts/python/validate_schema.py
validating 31 object YAMLs ...
OK: all schemas pass

$ python scripts/python/build_graph.py
wrote knowledge/graph/graph.json: 31 nodes, 86 edges

$ python scripts/python/check_orphans.py
OK: no orphan nodes

$ mkdocs build --strict
Documentation built in 55.92 seconds   (no warnings; both en + zh locales)
```

## Warnings

- The added `tasks` keys `domain-adaptation` and `cross-magnification-evaluation` are new vocabulary items — not yet formalised in `knowledge/taxonomies/`. If we add another DA-flavoured paper, formalise these in a `task_taxonomy.yaml` entry.
- The `links.pdf` field is conventionally for direct PDF URLs; the PMC link is a landing page. Acceptable but worth flagging.
- The smartphone-microscopy and varying-tissue-content claims are taken from the paper's abstract; the paper itself contains the supporting figures and numbers, which were not extracted in detail here.

## Next Suggested Actions

1. Ingest `coudray-2018` and `kather-2019` (NSCLC + MSI mutation prediction) — both are seed papers for biomarker prediction from H&E and are cited frequently by every paper in this cohort.
2. Ingest the Bejnordi 2017 JAMA Camelyon16 challenge paper (`bejnordi-2017-camelyon16`) — would resolve a long-standing deferred reference from cluster 1.
3. Consider promoting `domain-adaptation` and `cross-magnification-evaluation` into the task taxonomy if these come up in another /add-article.
